rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Permitir que los usuarios autenticados puedan leer y escribir archivos
      allow read, write: if request.auth != null;
    }
  }
}
