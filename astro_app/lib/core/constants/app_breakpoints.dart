/// Breakpoints adaptativos — Material 3 guidelines.
abstract final class AppBreakpoints {
  /// Compact (teléfono vertical).
  static const double compact = 600;

  /// Medium (tablet vertical, fold abierto).
  static const double medium = 840;

  /// Expanded (tablet horizontal, desktop pequeño).
  static const double expanded = 1200;

  /// Large (desktop).
  static const double large = 1600;
}
